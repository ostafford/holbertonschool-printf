#include <stdarg.h>
#include <stdio.h>
#include "main.h"

/**
 * 
 *
 *
 */
 int _printf(const char *format, ...)
 {
 va_list ap;

va_start(ap, format);
count = 0;
while (*format != '\0')
{
if (*format == '%')
count += print_format (*(format), ap);
else
count += write (1, format, 1);
++format; 

}
}